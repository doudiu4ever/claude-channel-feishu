{
  "mcpServers": {
    "feishu": {
      "command": "bash",
      "args": [
        "-c",
        "if [ -n \"$CLAUDE_PLUGIN_ROOT\" ]; then cd \"$CLAUDE_PLUGIN_ROOT\" && exec node dist/server.mjs; else { [ -d node_modules ] || npm install --silent --no-audit --no-fund --prefer-offline; } && exec ./node_modules/.bin/tsx server.ts; fi"
      ]
    }
  }
}
